# Auth API

The **Auth API** is the backend service responsible for user authentication, session management, and authorization for Techibbie. It exposes a REST API consumed by the Auth Frontend and other internal services.
